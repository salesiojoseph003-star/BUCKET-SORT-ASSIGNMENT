# Bucket Sort Algorithm

Bucket Sort divides elements into several buckets.

Steps:

1. Find minimum and maximum values
2. Create empty buckets
3. Place numbers into buckets according to their value range
4. Sort each bucket using insertion sort
5. Combine buckets to obtain the final sorted list