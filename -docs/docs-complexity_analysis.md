# Complexity Analysis

Best Case: O(n)
When elements distribute evenly among buckets.

Average Case: O(n)
Most buckets contain few elements.

Worst Case: O(n²)
All elements fall into one bucket and insertion sort handles all items.

Space Complexity: O(n + k)

n = number of elements
k = number of buckets